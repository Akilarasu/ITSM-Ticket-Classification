# Models Directory

This folder is intended for storing serialized model artifacts (e.g., .pkl, .joblib) in production environments.

Models are not stored in this repository to maintain lightweight version control.
